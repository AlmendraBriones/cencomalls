import  React from 'react';

const DefaultTemplate: React.FC = () => {
    return (
        <div>
            <h1>Default Template</h1>
            <p>This is the default template for the application.</p>
        </div>
    );
}
export default DefaultTemplate;